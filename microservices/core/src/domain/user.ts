// Domain barrel: user aggregate.
//
// Pure type re-export from the canonical zod schemas in
// `@ai-data-room/api-utils/schemas/auth-orgs`. Type-only — excluded
// from coverage. See `org.ts` for the rationale.

export type { User } from "@ai-data-room/api-utils/schemas/auth-orgs";
