# docs/

Engineering docs that live with the repo (not the product-level briefs in the upstream workspace).

- `deployment.md` — runbook (stub; fleshed out in slice 1 T-014).
- `next-steps-deployments.md` — template-inherited notes on SST deployment setup; archive / rewrite as we make the SST stages concrete.
- `briefs/` — product brief snapshot synced from the upstream workspace.
- `slices/` — per-slice traceability matrix (one file per completed slice).
- `privacy/` — privacy docs referenced from specs (PostHog posture etc.).
