node_modules
**/dist
**/dist/**
.sst
*.min.js
bun.lock
bun.lockb
coverage
CHANGELOG.md
sst-env.d.ts
**/sst-env.d.ts
# Drizzle-generated. Hand-formatting fights `drizzle-kit generate`.
packages/db/migrations/**/*.sql
packages/db/migrations/meta/**
# Bash scripts — not in prettier's parser set.
scripts/*.sh
# Local Claude Code state (gitignored; here so `prettier --check` on a
# fresh clone with `.claude/settings.local.json` doesn't fail).
.claude
