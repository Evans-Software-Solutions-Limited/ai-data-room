# Zod schemas

One file per feature slice. Keeps request/response contracts typed end to end between `microservices/core` handlers and `packages/web` consumers.

Populated as slice task phases land:

- `auth-orgs.ts` — slice 1
- `rooms.ts` — slice 2
- `access-control.ts` — slice 3
- `checklist.ts` — slice 4
- `sensecheck.ts` — slice 5
- `qna.ts` — slice 6
- `dashboard.ts` — slice 7
- `billing.ts` — slice 8
- `onboarding.ts` — slice 9
